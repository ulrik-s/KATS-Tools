name: Build installers

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  macos-pkg:
    name: Build macOS pkg
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute version
        id: version
        shell: bash
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="0.0.${GITHUB_RUN_NUMBER}"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Validate required files
        shell: bash
        run: |
          test -f assets/KATS-Tools.dotm
          test -f assets/KATSUpdater.applescript
          test -f pkg/scripts/postinstall

      - name: Prepare Apple signing material
        shell: bash
        env:
          APPLE_INSTALLER_CERT_P12_BASE64: ${{ secrets.APPLE_INSTALLER_CERT_P12_BASE64 }}
          APPLE_INSTALLER_CERT_PASSWORD: ${{ secrets.APPLE_INSTALLER_CERT_PASSWORD }}
          APPLE_API_KEY_P8_BASE64: ${{ secrets.APPLE_API_KEY_P8_BASE64 }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
        run: |
          echo "$APPLE_INSTALLER_CERT_P12_BASE64" | base64 --decode > "$RUNNER_TEMP/installer-cert.p12"
          echo "$APPLE_API_KEY_P8_BASE64" | base64 --decode > "$RUNNER_TEMP/AuthKey_${APPLE_API_KEY_ID}.p8"

      - name: Import Apple installer certificate
        shell: bash
        env:
          APPLE_INSTALLER_CERT_PASSWORD: ${{ secrets.APPLE_INSTALLER_CERT_PASSWORD }}
        run: |
          KEYCHAIN_PASSWORD="$(openssl rand -hex 16)"
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -lut 21600 build.keychain
          security import "$RUNNER_TEMP/installer-cert.p12" \
            -k build.keychain \
            -P "$APPLE_INSTALLER_CERT_PASSWORD" \
            -T /usr/bin/productsign
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD" build.keychain
          security find-identity -v build.keychain

      - name: Build unsigned pkg payload
        shell: bash
        run: |
          chmod +x pkg/scripts/postinstall
          mkdir -p "build/payload/Library/Application Support/KATS-Tools"
          cp "assets/KATS-Tools.dotm" "build/payload/Library/Application Support/KATS-Tools/KATS-Tools.dotm"
          cp "assets/KATSUpdater.applescript" "build/payload/Library/Application Support/KATS-Tools/KATSUpdater.applescript"
          cp "assets/KATSUpdater.bat" "build/payload/Library/Application Support/KATS-Tools/KATSUpdater.bat"

          pkgbuild \
            --root "build/payload" \
            --identifier "se.example.kats-tools" \
            --version "${{ steps.version.outputs.version }}" \
            --install-location "/" \
            --scripts "pkg/scripts" \
            "build/KATS-Tools-unsigned.pkg"

      - name: Sign pkg
        shell: bash
        env:
          APPLE_INSTALLER_IDENTITY: ${{ secrets.APPLE_INSTALLER_IDENTITY }}
        run: |
          productsign \
            --sign "$APPLE_INSTALLER_IDENTITY" \
            "build/KATS-Tools-unsigned.pkg" \
            "build/KATS-Tools-${{ steps.version.outputs.version }}.pkg"

      - name: Notarize pkg
        shell: bash
        env:
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
        run: |
          xcrun notarytool submit "build/KATS-Tools-${{ steps.version.outputs.version }}.pkg" \
            --key "$RUNNER_TEMP/AuthKey_${APPLE_API_KEY_ID}.p8" \
            --key-id "$APPLE_API_KEY_ID" \
            --issuer "$APPLE_API_ISSUER_ID" \
            --wait

          xcrun stapler staple "build/KATS-Tools-${{ steps.version.outputs.version }}.pkg"

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-pkg-${{ steps.version.outputs.version }}
          path: build/KATS-Tools-${{ steps.version.outputs.version }}.pkg

  windows-installer:
    name: Build Windows installer
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute version
        id: version
        shell: pwsh
        run: |
          if ($env:GITHUB_REF_TYPE -eq "tag") {
            $version = $env:GITHUB_REF_NAME -replace '^v',''
          } else {
            $version = "0.0.$env:GITHUB_RUN_NUMBER"
          }
          "version=$version" >> $env:GITHUB_OUTPUT

      - name: Validate required files
        shell: pwsh
        run: |
          if (!(Test-Path assets/KATS-Tools.dotm)) { throw "Missing assets/KATS-Tools.dotm" }
          if (!(Test-Path assets/KATSUpdater.bat)) { throw "Missing assets/KATSUpdater.bat" }
          if (!(Test-Path windows/KATS-Tools.iss)) { throw "Missing windows/KATS-Tools.iss" }

      - name: Install Inno Setup
        shell: pwsh
        run: |
          choco install innosetup --no-progress -y

      - name: Build installer
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build\windows | Out-Null

          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (!(Test-Path $iscc)) { throw "ISCC.exe not found" }

          & $iscc `
            "/DMyAppVersion=${{ steps.version.outputs.version }}" `
            "/Obuild\windows" `
            "/FKATS-Tools-Setup-${{ steps.version.outputs.version }}" `
            "windows\KATS-Tools.iss"

      - name: Prepare signing certificate
        shell: pwsh
        env:
          WINDOWS_SIGN_CERT_PFX_BASE64: ${{ secrets.WINDOWS_SIGN_CERT_PFX_BASE64 }}
        run: |
          [IO.File]::WriteAllBytes(
            "$env:RUNNER_TEMP\kats-sign.pfx",
            [Convert]::FromBase64String($env:WINDOWS_SIGN_CERT_PFX_BASE64)
          )

      - name: Sign installer
        shell: pwsh
        env:
          WINDOWS_SIGN_CERT_PASSWORD: ${{ secrets.WINDOWS_SIGN_CERT_PASSWORD }}
        run: |
          $signtool = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin" -Recurse -Filter signtool.exe |
            Sort-Object FullName -Descending |
            Select-Object -First 1 -ExpandProperty FullName

          if (-not $signtool) { throw "signtool.exe not found" }

          & $signtool sign `
            /fd SHA256 `
            /td SHA256 `
            /tr "http://timestamp.digicert.com" `
            /f "$env:RUNNER_TEMP\kats-sign.pfx" `
            /p "$env:WINDOWS_SIGN_CERT_PASSWORD" `
            "build\windows\KATS-Tools-Setup-${{ steps.version.outputs.version }}.exe"

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe-${{ steps.version.outputs.version }}
          path: build/windows/KATS-Tools-Setup-${{ steps.version.outputs.version }}.exe

